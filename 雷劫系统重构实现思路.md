# 雷劫系统重构——详细实现思路

> 记录日期：2026-08-17
> 分支：tribulation-rework（基于 main 117efc0）
> 前置：`雷劫系统重构方案.md`（数值总表 + 综合评判 + 扩展性方向）
> 说明：本文档按**实现依赖顺序**给出逐步重构方案；所有硬编码数值抽为常量；配置接口留待项目完成后实现（本阶段用代码常量）。

---

## 〇、总原则

1. **数据驱动劫谱**：波数/道数/伤害/间隔/特效从 `Realm` 三个 switch 抽为 `TribulationSpec`（代码常量表，不 JSON 化——配置接口后续阶段做）。
2. **劫种抽象**：雷劫逻辑从 `TribulationHandler` 抽出为 `TribulationType` 接口 + 雷劫实现，未来可加心魔劫/风火劫。
3. **事件钩子**：渡劫生命周期发布 Forge 事件（开始/雷击/波末/成功/失败），供其他系统监听。
4. **防御系数接入**：宗门护盾/雷灵根减免等统一切入 `TribulationDefense` 链，新防御可插拔。
5. **常量集中**：所有硬编码数值（间隔、半径、粒子数、系数、上限）抽到 `TribulationConstants`。

---

## 一、实现依赖顺序（按此逐步执行，每步编译验证）

```
Step 1: TribulationConstants（常量集中）
Step 2: TribulationSpec（数据驱动劫谱：波数/道数/伤害/间隔/特效）
Step 3: TribulationType（劫种抽象：接口 + 雷劫实现）
Step 4: TribulationDefense（防御链：宗门护盾 / 雷灵根减免）
Step 5: 渡劫事件（Started / BoltStrike / WaveEnd / Succeeded / Failed）
Step 6: CultivationData 接入（spec/type/defense 状态）
Step 7: TribulationHandler 重构（主循环按 spec+type+defense+事件）
Step 8: Realm 接入 spec 表 + 数值常量
Step 9: 编译 + 构建 + 部署验证
```

---

## 二、Step 1：TribulationConstants（常量集中）

新建 `com.friday.cultivation.event.tribulation.TribulationConstants`：

```java
public final class TribulationConstants {
    private TribulationConstants() {}

    // 通用
    public static final int WAVE_COOLDOWN_TICKS = 20;          // 波间冷却
    public static final double BOLT_RANDOM_RADIUS = 1.0;       // 雷击随机半径
    public static final int SUB_WAVE_DURATION_BASE = 30;       // 子波基准时长
    public static final int MIN_SUB_BOLT_INTERVAL = 4;         // 最小雷击间隔
    public static final int MAX_SUB_BOLT_INTERVAL = 20;        // 最大雷击间隔
    public static final float CLOUD_SOUND_VOLUME = 0.8f;
    public static final float CLOUD_SOUND_PITCH = 0.42f;
    public static final float THUNDER_SOUND_VOLUME = 1.3f;
    public static final float THUNDER_SOUND_PITCH = 0.55f;
    public static final int CLOUD_PARTICLE_COUNT = 34;
    public static final double CLOUD_PARTICLE_RADIUS = 4.8;
    public static final double CLOUD_PARTICLE_HEIGHT = 0.28;
    public static final double CLOUD_PARTICLE_SPREAD = 3.4;
    public static final double CLOUD_PARTICLE_SPEED = 0.012;
    public static final int CLOUD_CLOUD_COUNT_COOLDOWN = 8;
    public static final int CLOUD_CLOUD_COUNT_NORMAL = 5;
    public static final double CLOUD_RADIUS = 5.6;
    public static final double CLOUD_Y_OFFSET = 8.0;

    // 雷灵根减免
    public static final int LIGHTNING_ROOT_DIVISOR = 2;

    // 综合评判权重（灵根/体质/功法品质）
    public static final double SPIRIT_ROOT_WEIGHT = 0.5;
    public static final double PHYSIQUE_WEIGHT = 0.3;
    public static final double TECHNIQUE_WEIGHT = 0.2;
}
```

---

## 三、Step 2：TribulationSpec（数据驱动劫谱）

新建 `com.friday.cultivation.event.tribulation.TribulationSpec`：

```java
/** 渡劫配置（数据驱动劫谱）：波数/道数/伤害/间隔/特效 */
public record TribulationSpec(
    int waves,                  // 波数
    int boltsPerWave,           // 每波道数
    int strikeDamage,           // 单次伤害（0 = 用标准生命×比例）
    double damageRatio,         // 伤害占标准生命比例（strikeDamage<=0 时用）
    int boltIntervalTicks,      // 雷击间隔（0 = 自动按道数计算）
    TribulationType type        // 劫种
) {
    /** 便捷：从 Realm 数值构造（保留现有 switch 数值，后续替换为常量表） */
    public static TribulationSpec of(int waves, int boltsPerWave, int strikeDamage) {
        return new TribulationSpec(waves, boltsPerWave, strikeDamage, 0.0, 0, TribulationType.LIGHTNING);
    }
}
```

`TribulationType` 先声明为枚举（Step 3 扩展为接口+实现）：

---

## 四、Step 3：TribulationType（劫种抽象）

新建 `com.friday.cultivation.event.tribulation.TribulationType`：

```java
/** 劫种抽象：每种劫独立的表现与伤害结算 */
public interface TribulationType {
    String id();

    /** 生成劫的表现（闪电/火焰/黑雾…） */
    void spawnEffect(ServerLevel level, ServerPlayer player, TribulationSpec spec);

    /** 伤害结算（雷=闪电伤害；心魔=百分比扣血…） */
    void applyDamage(ServerLevel level, ServerPlayer player, TribulationSpec spec, int strikeDamage);

    /** 默认雷劫实现 */
    TribulationType LIGHTNING = new LightningTribulation();

    final class LightningTribulation implements TribulationType { ... } // 从 TribulationHandler 抽出现有逻辑
}
```

当前只实现雷劫，心魔劫等留接口。

---

## 五、Step 4：TribulationDefense（防御系数接入）

新建 `com.friday.cultivation.event.tribulation.TribulationDefense`：

```java
/** 渡劫防御链：按注册顺序依次减免伤害 */
public interface TribulationDefense {
    /** 返回减免后的伤害（≤0 表示完全免疫） */
    float reduce(float damage, ServerPlayer player, CultivationData data);

    // 注册表
    static final List<TribulationDefense> DEFENSES = new ArrayList<>();
    static void register(TribulationDefense d) { DEFENSES.add(d); }
    static float applyAll(float damage, ServerPlayer player, CultivationData data) {
        float dmg = damage;
        for (TribulationDefense d : DEFENSES) dmg = d.reduce(dmg, player, data);
        return Math.max(0.0f, dmg);
    }
}
```

实现：
- `SectDomeTribulationDefense`：宗门护盾吸收（从 `SectProtectionDomeHandler.absorbTribulationDamage` 移入）
- `LightningRootDefense`：雷灵根 ÷2（从 `currentStrikeDamage` 硬编码移入）
- 在 `TribulationHandler` 静态块注册

---

## 六、Step 5：渡劫事件（Event Hooks）

新建 5 个 Forge 事件（`com.friday.cultivation.event.tribulation`）：

```java
public class TribulationStartedEvent extends PlayerEvent {   // 携带 spec/总道数
    public class TribulationBoltStrikeEvent extends PlayerEvent { // 可 setDamage 修改
    public class TribulationWaveEndEvent extends PlayerEvent {    // 携带剩余波数
    public class TribulationSucceededEvent extends PlayerEvent {  // 携带新境界
    public class TribulationFailedEvent extends PlayerEvent {     // 携带掉落境界
```

TribulationHandler 各节点 `MinecraftForge.EVENT_BUS.post(...)`；现有 `grantRealmAdvancement`、`LooseImmortalHandler` 可改走事件或保留直调+额外发事件。

---

## 七、Step 6：CultivationData 接入

```java
// 新增字段
private TribulationType tribulationType = TribulationType.LIGHTNING;
private int tribulationSpecWaves = 0;        // 当前渡劫波数（来自 spec）
private double tribulationDamageRatio = 0.0; // 伤害比例（来自 spec）

// startTribulation 重载：接收 TribulationSpec
public void startTribulation(TribulationSpec spec) { ... }
```

现有 `startTribulation(int strikes, int strikeDamageOverride, int boltsPerWave)` 保持兼容（内部转 spec）。

---

## 八、Step 7：TribulationHandler 重构

- `tickPendingTribulationWave`：改用 spec 的波数/道数/间隔
- `currentStrikeDamage`：改用 spec 伤害（含比例计算 + 防御链）
- `spawnTribulationBolt` / `applyTribulationBoltDamage`：按 type 分发
- `beginTribulationInternal`：按 spec 启动 + 发 Started 事件
- 各节点发 WaveEnd / BoltStrike / Succeeded / Failed 事件
- 常量化：`WAVE_COOLDOWN` 等用 TribulationConstants

---

## 九、Step 8：Realm 接入 spec 表

`Realm` 新增：

```java
/** 该境界当前子阶段的渡劫配置（无渡劫返回 null） */
public TribulationSpec tribulationSpec(SubStage stage) {
    // 用常量表替代 switch 数值（数值见 雷劫系统重构方案.md 第二节）
}
```

`tribulationCount()/tribulationBoltsPerWave()/tribulationStrikeDamage()` 保留（兼容调用方），内部委托 spec 或标记 deprecated。

---

## 十、综合评判接入（灵根/体质/功法品质）

新建 `TribulationScalingHelper`：

```java
/** 综合评判：越好的灵根/体质/功法 → 渡劫越多 */
public static double scalingMultiplier(Player player, CultivationData data) {
    double spiritRoot = spiritRootQuality(data);   // 0~0.5
    double physique = physiqueQuality(data);       // 0~0.3
    double technique = techniqueQuality(player);   // 0~0.2
    return 1.0 + spiritRoot + physique + technique;
}

// 最终道数 = round(基准道数 × multiplier)
```

权重常量放 TribulationConstants。**品质分级映射**：先做简化版（如按枚举 ordinal/稀有度分段），细节待确认。

---

## 十一、常量与配置说明

- 本阶段所有数值为**代码常量**（TribulationConstants + 各 spec），统一修改点
- **配置接口（JSON/配置文件）留待项目完成后实现**，本阶段不做
- 新增境界/调数值：改 Realm.tribulationSpec 常量表 + TribulationConstants

---

## 十二、验证点

- [ ] 编译通过（每 Step 后 gradlew compileJava）
- [ ] 渡劫成功/失败/散仙劫行为不变（回归）
- [ ] 宗门护盾吸收仍生效（防御链）
- [ ] 雷灵根 ÷2 仍生效
- [ ] 事件可被监听（简单测试或代码走查）
- [ ] 数值与方案文档一致
