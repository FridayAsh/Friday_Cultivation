# 修仙飞行已知问题（2026-08-14）

## 1. 灵气飞行/御剑飞行不消耗灵气

- **现象**：灵气飞行激活后灵气数值不下降，HUD 灵气条不变。
- **调查状态**：
  - `CultivationFlightHandler.tickFlight` 已实现每 20 tick 扣除逻辑（灵气 25/秒、御剑 20/20tick，经 `applySpellQiCostMultiplier` 计算，扣除后 `syncToClient`）。
  - `CultivationFlightEvents.onPlayerTick` 仅对 `ServerPlayer` 调用 `tickFlight`（客户端不执行）。
  - `QiFlightTogglePacket` 注册正常，双击空格调用 `toggleQiFlight` → `setQiFlightToggled(true)`。
  - 待验证：`tickFlight` 中 `player.tickCount % 20 == 0` 是否真正命中；`canQiFlight` 服务端返回值；`setCurrentQi` 后 HUD 是否同步。
