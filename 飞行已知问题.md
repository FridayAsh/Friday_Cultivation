# 修仙飞行当前分支已知问题（2026-08-13）

当前修仙飞行（御剑飞行 + 灵气飞行）存在以下 3 个已知问题，尚未修复：

## 1. 两种飞行都不消耗灵气
- **灵气飞行**：`tickFlight` 中开关开启（`isQiFlightToggled`）应每 20 tick 扣 25 灵气，但实际不扣除（服务端 `setCurrentQi` 后客户端 HUD 不更新/未正确同步）。
- **御剑飞行**：`tickFlight` 中无灵气消耗逻辑（`toggleSwordFlight` 时未记录并持续扣灵气）。

## 2. 御剑飞行不再在玩家脚底渲染对应模型
- 删除了 `SwordFlightClientRenderer`，新建的 `CultivationSwordFlightRenderer` 存在但未生效——御剑激活状态 `isSwordFlightActive` 客户端判定依赖 `CultivationData` 同步，可能未正确同步导致渲染器条件不满足。

## 3. 御剑飞行停止时需要额外准备一把剑
- `toggleSwordFlight` 施放时取出主手剑存入 `SWORD_FLIGHT`，但停止时（再施放）检查当前主手是否持剑（`SwordItem`）——若主手已空则提示"御剑飞行需要主手持有剑"而无法停止，施法用的剑收不回来。
