# 问题记录：渡劫加成境界继承 + 生命值浮动

> 记录日期：2026-08-17
> 分支：tribulation-rework
> 状态：已定位根因，部分已修复，部分待实施

---

## 问题一：令牌降境界后渡劫加成未清除

### 现象
- 使用境界令牌降低境界后，渡劫隐藏加成仍然存在（未按"低于渡劫后境界取消加成"规则清除）
- 退出存档重进后加成消失（已通过 NBT 持久化修复）

### 预期规则
1. 突破渡劫 → 根据渡劫时的属性值计算加成数值
2. 记录渡劫突破后的境界
3. 获得加成
4. 低于渡劫突破后的境界 → 取消加成

### 根因分析
- 渡劫奖励记录境界：`completeBreakthrough` 中 `advanceOnSuccess()` 先执行（境界已提升到渡劫后），之后 `addTribulationBonus(percent, data.getRealm())` 记录的是**渡劫后境界** ✓ 正确
- `setRealm`（令牌切境界）有 `removeIf(e -> realm.ordinal() < e[1])` 清除低于境界的 entry ✓ 逻辑正确
- `activeTribulationMultiplier()` 只在 `realm >= minRealm` 时计入 ✓ 正确
- **疑点**：令牌降境界后加成仍在，可能因测试时序（onPlayerTick 未及时重算）或降到的境界未低于渡劫后境界

### 修复状态
- ✅ NBT 持久化：`tribulationBonusEntries` 保存/加载到存档（serializeNBT/deserializeNBT），修复"退出重进加成消失"
- ⏳ 令牌降境界清除：逻辑已正确（setRealm removeIf + activeTribulationMultiplier 判定），待实际验证

---

## 问题二：进入存档生命值浮动 20-30

### 现象
- 每次进入存档时，玩家 MAX_HEALTH 浮动 20-30
- 用户怀疑是随机数算法，要求彻查项目各文件

### 彻查结论（oracle 分析）
**全项目无随机数影响 MAX_HEALTH**——所有 HP 计算输入均为确定性（功法/灵根/真元/道基/金丹/散仙/境界标准/突破累计/渡劫加成全部确定性）。

### 根因（三处问题）

#### 1. 渡劫加成双重叠加（设计问题）
`ZhenyuanBonusHelper.constitutionPoints` 内部已乘 `hiddenMult`（渡劫乘数），而 `TechniqueEffectHandler` 又用 `UUID_TRIBULATION_HP_MULT` 作为 MULTIPLY_TOTAL 全局乘——**双重 dip**，导致 HP 偏高（但确定性）。

```java
// ZhenyuanBonusHelper.constitutionPoints
return (int) Math.round(d.getAttrConstitution() * hiddenMult(d));  // 已乘
// TechniqueEffectHandler
applyAttributeModifier(MAX_HEALTH, UUID_TRIBULATION_HP_MULT, activeTribulationMultiplier()-1.0, MULTIPLY_TOTAL);  // 又乘
```

**修复**：`constitutionPoints` 不应乘 `hiddenMult`（MULTIPLY_TOTAL 已全局应用）。

#### 2. applyAttributeModifier 用 `==` 比较 double
`TechniqueEffectHandler.applyAttributeModifier` 用 `existing.getAmount() == value` 判断是否需更新。浮点精度差异导致每 tick remove/re-add，属性值短暂波动。

**修复**：改为 epsilon 比较 `Math.abs(existing.getAmount() - value) < 1e-9`。

#### 3. 可能根因：applyZhenyuanMajorAutoRebalanceMigration 重复触发
`CapabilityEvents.onPlayerLogin` 调用 `applyZhenyuanMajorAutoRebalanceMigration`，若 `zhenyuanMajorAutoRebalanceApplied` 标志未正确持久化，每次加载都会增加真元属性 → 生命上升 20-30。

**验证**：检查存档 NBT 中 `zhenyuanMajorAutoRebalanceApplied` 是否为 true。

### 修复建议
1. **高优先级**：修复 applyAttributeModifier 的 double 比较（`==` → epsilon）
2. **高优先级**：消除渡劫加成双重叠加（constitutionPoints 不乘 hiddenMult）
3. **中优先级**：排查 applyZhenyuanMajorAutoRebalanceMigration 是否重复触发（加日志确认）
4. **低优先级**：addPermanentModifier 改 addTransientModifier（避免 NBT 冗余 + stale modifier）

### 修复状态
- ⏳ 待实施（oracle 已给出根因与修复方案）

---

## 附：相关文件
- `event/TechniqueEffectHandler.java`：MAX_HEALTH modifier 应用链、applyAttributeModifier
- `cultivation/CultivationData.java`：tribulationBonusEntries、activeTribulationMultiplier、applyZhenyuanMajorAutoRebalanceMigration
- `cultivation/ZhenyuanBonusHelper.java`：constitutionPoints 等五维加成
- `event/CapabilityEvents.java`：onPlayerLogin 触发迁移
