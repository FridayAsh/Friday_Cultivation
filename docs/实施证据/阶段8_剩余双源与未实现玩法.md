# 阶段 8：剩余双源与未实现玩法收敛证据

基线：`cb3ce89f7deae3165ea35e01567b180731c21138`

## 已完成

1. 锻体生命继承只由 `CultivationData.bodyTemperingHpInherited` 提供。旧 PersistentData 键只在首次加载且 Capability 尚无值时读取，读取后立即删除；新逻辑不再写入旧键，转世清理也只清理迁移键。
2. 普通突破奖励结算规则已写入实施文档决策记录，并同步到字段注释；`RealmTransition` 先应用奖励再按最终 `getMaxQi()` 补满灵气，`TribulationHandler` 刷新最终生命上限后补满生命。
3. 元素旧空接口已删除：`getElementCount`、`getTotalElementQi`、`getElementPercent`、`getElementDamageBonus`、`getElementPowerPercent`、`getDominantElement`。FireSwordAura 改读装备功法主元素，蓄力火球改读 `TechniqueBonusHelper.spellElementMultiplier`，旧 UI 百分比渲染方法删除。
4. 练气极境未实现字段和入口已删除：`daoFruitTotalEaten`、相关判断/递增/进入方法、NBT/Clone 读写及 TODO 均不存在；没有通过清空存档规避问题。
5. 散仙劫配置收敛：`Realm.LOOSE_IMMORTAL` 的通用配置返回“不可表达等级”的空配置，实际等级参数只由 `LooseImmortalBonusHelper` 读取和生成，避免 Realm 与 Handler 两套数值覆盖。
6. 区块灵气再生保留小数余量，`peek` 无副作用，并由 `ChunkQiPoolTest` 验证高频读取不会压制再生。

## 自动验证

- `gradlew test`：19 个测试通过。
- 静态检查：上述旧元素 API、极境字段、`BlockQiMapping`、`tribulationBoltInterval` 均无源码调用者；普通突破注释与实现一致。

## 未在本阶段处理的边界

元素灵气和练气极境的删除属于文档决策，不新增替代玩法数值；若后续重新设计，必须先新增决策记录和唯一数据契约，不能恢复第二套旧接口。
