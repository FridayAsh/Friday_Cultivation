# 境界体系重构——详细实现思路

> 记录日期：2026-08-17
> 状态：实现前设计，明天实现
> 前置：`境界体系重构方案.md` 已确认数值设计；本文件给出**具体到文件/方法/改法**的实现思路

---

## 〇、核心风险与总体策略

**风险**：`Realm` 枚举被 42 个文件引用，其中 22 个文件使用 `ordinal()` 假设（如 `Realm.values()[idx]`、`ordinal()*10`）。散仙移位 + 新增 4 境界若改变枚举声明顺序，会破坏全部 ordinal 假设。

**总体策略（最小破坏）**：
1. **新增 4 境界（玄仙/仙君/仙尊/仙王）追加到枚举声明末尾**——不改变现有 17 个枚举值的 ordinal，22 个 ordinal 文件零破坏。
2. **散仙"比真仙低级"通过逻辑顺序实现，不改声明顺序**——散仙仍是独立旁支枚举值，通过 `progressIndex` 与显示排序调整让它在真仙之前。
3. **主链路 `next()` 调整为**：渡劫 → 真仙 → 玄仙 → 仙君 → 仙尊 → 仙王 → 半圣（新增 4 个插入），散仙仍旁支跳过。
4. **所有 switch 全枚举处**补新增 4 个 case（编译会强制，逐个处理）。

---

## 一、Realm.java 改动（核心）

### 1.1 枚举声明（追加 4 个，不改变现有顺序）

```java
public enum Realm {
    MORTAL("mortal"), BODY_TEMPERING("body_tempering"), QI_REFINING("qi_refining"),
    FOUNDATION_BUILDING("foundation_building"), GOLDEN_CORE("golden_core"),
    NASCENT_SOUL("nascent_soul"), SOUL_FORMATION("soul_formation"),
    VOID_REFINING("void_refining"), BODY_INTEGRATION("body_integration"),
    MAHAYANA("mahayana"), TRIBULATION_TRANSCENDENCE("tribulation_transcendence"),
    TRUE_IMMORTAL("true_immortal"), HALF_SAGE("half_sage"), SAGE("sage"),
    HALF_EMPEROR("half_emperor"), GREAT_EMPEROR("great_emperor"),
    LOOSE_IMMORTAL("loose_immortal"),
    // 新增（追加末尾，不动上面 ordinal）
    MYSTIC_IMMORTAL("mystic_immortal"),   // 玄仙
    IMMORTAL_LORD("immortal_lord"),       // 仙君
    IMMORTAL_VENERABLE("immortal_venerable"), // 仙尊
    IMMORTAL_KING("immortal_king");       // 仙王
}
```

**关键**：新增的 4 个**追加末尾**，原有 17 个 ordinal 0-16 不变 → 22 个 ordinal 文件零破坏。

### 1.2 子阶段（9 小境界，复用真仙 heaven_1~9）

```java
public int subStageCount() {
    // 新增 case：MYSTIC_IMMORTAL, IMMORTAL_LORD, IMMORTAL_VENERABLE, IMMORTAL_KING -> 9
}

public boolean usesNumericLevels() {
    // 新增 4 个境界返回 true（与 TRUE_IMMORTAL 一致）
}

public SubStage subStageAt(int level) {
    // 新增：MYSTIC_IMMORTAL / IMMORTAL_LORD / IMMORTAL_VENERABLE / IMMORTAL_KING
    //   level 1-9 -> new SubStage("heaven_" + level, level)  // 复用真仙的一到九重天
}
```

### 1.3 主链路 next()（插入新境界）

```java
public Realm next() {
    // 主链路：凡人→…→渡劫→真仙→玄仙→仙君→仙尊→仙王→半圣→圣人→半帝→大帝
    // 散仙(LOOSE_IMMORTAL)与大帝为终点
    // 改为按显式链表而非 ordinal：
    //   TRIBULATION_TRANSCENDENCE -> TRUE_IMMORTAL
    //   TRUE_IMMORTAL -> MYSTIC_IMMORTAL
    //   MYSTIC_IMMORTAL -> IMMORTAL_LORD
    //   IMMORTAL_LORD -> IMMORTAL_VENERABLE
    //   IMMORTAL_VENERABLE -> IMMORTAL_KING
    //   IMMORTAL_KING -> HALF_SAGE
    //   （其余保持 ordinal 顺序，跳过 LOOSE_IMMORTAL）
}
```

**注意**：`next()` 当前用 ordinal 递增 + 跳过 LOOSE_IMMORTAL。改造后需**显式 switch 链表**，避免依赖声明顺序（因为新境界在末尾而逻辑上在真仙与半圣之间）。

### 1.4 progressIndex（散仙移到真仙之前 + 新境界插入）

当前 `progressIndex` 用 `Realm.values()` 累加，且 LOOSE_IMMORTAL 特殊返回 38。改造为**显式顺序表**：

```java
// 定义逻辑顺序（显示/进度用）：
// MORTAL, BODY_TEMPERING, QI_REFINING, FOUNDATION_BUILDING, GOLDEN_CORE,
// NASCENT_SOUL, SOUL_FORMATION, VOID_REFINING, BODY_INTEGRATION, MAHAYANA,
// TRIBULATION_TRANSCENDENCE, LOOSE_IMMORTAL,   // 散仙在真仙之前
// TRUE_IMMORTAL, MYSTIC_IMMORTAL, IMMORTAL_LORD, IMMORTAL_VENERABLE, IMMORTAL_KING,
// HALF_SAGE, SAGE, HALF_EMPEROR, GREAT_EMPEROR
```

### 1.5 标准生命值（新增方法，替代 baseHealthForNpc 的玩家用法）

```java
/** 标准生命值（凡人 100 → 大帝 8000），玩家 MAX_HEALTH 基础与加成基准 */
public double standardMaxHealth() {
    return switch (this) {
        case MORTAL -> 100.0;
        case BODY_TEMPERING -> 150.0;
        case QI_REFINING -> 220.0;
        case FOUNDATION_BUILDING -> 320.0;
        case GOLDEN_CORE -> 460.0;
        case NASCENT_SOUL -> 620.0;
        case SOUL_FORMATION -> 820.0;
        case VOID_REFINING -> 1080.0;
        case BODY_INTEGRATION -> 1400.0;
        case MAHAYANA -> 1780.0;
        case TRIBULATION_TRANSCENDENCE -> 2200.0;
        case LOOSE_IMMORTAL -> 2600.0;
        case TRUE_IMMORTAL -> 3100.0;
        case MYSTIC_IMMORTAL -> 3700.0;
        case IMMORTAL_LORD -> 4400.0;
        case IMMORTAL_VENERABLE -> 5200.0;
        case IMMORTAL_KING -> 6100.0;
        case HALF_SAGE -> 6600.0;
        case SAGE -> 7100.0;
        case HALF_EMPEROR -> 7600.0;
        case GREAT_EMPEROR -> 8000.0;
    };
}
```

### 1.6 雷劫（新增 4 境界 case）

```java
public int tribulationCount(SubStage stage) {
    // MYSTIC_IMMORTAL（玄仙）：与 TRUE_IMMORTAL 一致
    //   level 3 -> 3 波, level 6 -> 6 波, level 9 -> 9 波
    // IMMORTAL_LORD（仙君）：每个小境界都渡劫
    //   level 1-9 -> 3 波（每波 9 道）
    // IMMORTAL_VENERABLE（仙尊）：level 1-9 -> 4 波
    // IMMORTAL_KING（仙王）：level 1-9 -> 5 波；level 9 -> 9 波（圆满→半圣）
}

public int tribulationBoltsPerWave(SubStage stage) {
    // MYSTIC_IMMORTAL, IMMORTAL_LORD, IMMORTAL_VENERABLE, IMMORTAL_KING -> 9
}

public int tribulationStrikeDamage() {
    // MYSTIC_IMMORTAL -> 185, IMMORTAL_LORD -> 190,
    // IMMORTAL_VENERABLE -> 195, IMMORTAL_KING -> 200
}
```

### 1.7 其余 switch 补 case（编译强制）

- `maxQi`：新境界按序递增（参考 TRUE_IMMORTAL 19000+，玄仙起 ~21000 逐境递增）
- `baseLifespan`：玄仙~仙王 递增（真仙 100 万 → 玄仙 120 万 → … → 仙王 180 万）
- `absorbMultiplier`：真仙 11 → 玄仙 12 → 仙君 13 → 仙尊 14 → 仙王 15 → 半圣 12（现有）需重排
- `qiShieldReductionPercent`：真仙 100 → 新增 4 个均 100
- `npcCategoryTranslationKey`：`ordinal() >= TRUE_IMMORTAL.ordinal()` 判断——新增境界 ordinal 更大，会自动归入 "immortal" 类（OK，无需改）

---

## 二、CultivationData.java 改动

### 2.1 锻体加成固定值（继承到后续，不重算）

```java
/** 锻体加成固定生命值（锻体境界时按标准值×锻体百分比计算一次，永久继承） */
private double bodyTemperingHpInherited = 0.0;

// 在突破/境界变更时计算一次：
// if (realm == BODY_TEMPERING) bodyTemperingHpInherited = 150.0 * bodyTemperingPercent(level);
// 突破到更高境界后：保持 bodyTemperingHpInherited 不变（继承），不重新计算

// 锻体百分比（按层数）：
// 1层+10% 2层+20% 3层+30% 4层+45% 5层+60% 6层+75% 7层+95% 8层+115% 9层+135% 10层+185%
private static double bodyTemperingPercent(int level) { ... }
```

### 2.2 标准生命值接入

玩家 MAX_HEALTH 基础值 = `realm.standardMaxHealth()`（替换当前"基础 20"）。

---

## 三、TechniqueEffectHandler.java 改动

### 3.1 真元体质加成百分比化（每点 +标准值 2%）

```java
// 当前：constitutionHpBonus = attrConstitution * 10（固定 +10/点）
// 改为：zhenyuanHp = standardMaxHealth * (attrConstitution * 0.02)   // 每点 +2%
// 仍 ÷4 传入，×4 还原（保持全局 ×4 结构）
```

### 3.2 突破累计加成百分比化（每次 +标准值 5%）

```java
// 当前：breakthroughHpBonus 固定累加（大帝封顶 2000/次）
// 改为：每次突破时 + standardMaxHealth(当前境界) * 0.05
```

### 3.3 锻体加成只作用生命值（继承值）

```java
// 当前：bodyTemperingHpBonus 每 tick 计算
// 改为：使用 CultivationData.bodyTemperingHpInherited（固定继承值，ADDITION）
//   applyAttributeModifier(UUID_BODY_TEMPERING_HP, data.bodyTemperingHpInherited / 4.0, ADDITION)
// 不再每 tick 重算；只作用 MAX_HEALTH，不作用攻击/盔甲/韧性/击退抗性
```

### 3.4 全局 ×4 倍率处理

- **当前**：`UUID_REALM_HP_MULT = 3.0 MULTIPLY_TOTAL`（=×4），作用于"基础20+全部ADDITION"
- **改造后**：基础值改为 standardMaxHealth，×4 倍率**保留**（方案确认过保留）。最终 = (standardMaxHealth + 真元% + 锻体继承 + 突破% + 功法 + 灵根 + …) × 4
- **功法/灵根暂不动**：维持现有固定值逻辑，后续再改

---

## 四、各 Handler 改动（真元/锻体/突破）

| 文件 | 改动 |
|---|---|
| `ZhenyuanBonusHelper.constitutionHpBonus` | 改为按 standardMaxHealth × (体质×2%) |
| `TechniqueEffectHandler` 锻体计算 | 改用继承固定值，只作用生命值 |
| `TechniqueEffectHandler` 突破累计 | 每次 +标准值5% |
| `CultivationData.advanceOnSuccess`（突破） | 突破时计算锻体继承值（若在锻体境界）/ 记录突破加成 |
| `RealmSelectionPacket` / 令牌切境界 | refreshMaxHealth 同步新标准值 |

---

## 五、lang 文件

`zh_cn.json` / `en_us.json` 新增：
- `realm.friday_cultivation.mystic_immortal` = 玄仙
- `realm.friday_cultivation.immortal_lord` = 仙君
- `realm.friday_cultivation.immortal_venerable` = 仙尊
- `realm.friday_cultivation.immortal_king` = 仙王

---

## 六、实施顺序（明天照此执行）

1. **Realm.java**：追加 4 枚举 → 补全部 switch case → 改 next() 显式链表 → progressIndex 显式顺序 → 新增 standardMaxHealth() → 雷劫
2. **编译**：`gradlew compileJava`，修复所有 switch 缺 case
3. **CultivationData.java**：bodyTemperingHpInherited 字段 + 锻体百分比方法 + 突破时计算
4. **TechniqueEffectHandler.java**：真元%化、突破%化、锻体用继承值、基础值改 standardMaxHealth
5. **lang 文件**：新增 4 境界名称
6. **编译 + 构建**：`gradlew build`
7. **同步 jars → 提交推送 → 部署 PCL**
8. **联机数据兼容检查**：NBT 存档中 realm 用字符串 id 存储，追加枚举不影响读档；但 progressIndex/absorbMultiplier 变化需验证

---

## 七、风险与验证点

- [ ] 22 个 ordinal 文件：追加枚举不改变现有 ordinal → 零破坏（核心策略）
- [ ] `npcCategoryTranslationKey`：`ordinal() >= TRUE_IMMORTAL.ordinal()` 自动覆盖新境界 → 验证
- [ ] `progressIndex`：显式顺序表需与 UI 显示（CultivationScreen）一致
- [ ] NBT 存档兼容：realm 按字符串 id 存储 → 追加无影响
- [ ] 散仙逻辑位置：显示在真仙前，但不进主突破链（next 跳过）
- [ ] 功法/灵根加成：**本次不动**，保持现状
